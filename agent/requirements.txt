# NetMonitor Agent
websockets>=12.0
httpx>=0.27.0
python-dotenv>=1.0.0

# SNMP: identita' e topologia di apparati di qualunque marca (switch gestiti,
# access point). Wheel py3-none-any, quindi si installa anche su ARM a 32 bit
# senza compilare nulla.
pysnmp>=7.1.0
